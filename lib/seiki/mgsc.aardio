namespace seiki

class mgsc {
	ctor() {
		import seiki;
		this.path = ..io.joinpath(::GamePath, "config.dat")
		this.buffer = ..string.loadBuffer(path)
	};
	set_fullscreen = function(flag){
		this.buffer[29] = (flag === true)? 0x01: 0x00
	};
	save = function(){
		string.save(this.path, this.buffer)
	}
}